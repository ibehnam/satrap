{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "SatrapVerificationVerdict",
  "type": "object",
  "additionalProperties": false,
  "required": ["passed"],
  "properties": {
    "passed": {
      "type": "boolean",
      "description": "True if the implementation satisfies the step's done_when criteria; false otherwise."
    },
    "note": {
      "type": "string",
      "minLength": 1,
      "description": "Explain what failed and what to change next (recommended when passed=false)."
    }
  }
}

